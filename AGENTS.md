# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (only a `README.md`). There are no dependencies, services, build steps, lint checks, or tests to run.

When code is added, update this section with:
- How to install dependencies
- How to run the dev server / application
- How to run lint and tests
- Any non-obvious caveats for the development environment
